# Contributing

1. Read `docs/MCP-V2.md` and `skills/mcp-building/SKILL.md`.
2. Keep tool names `x_organic_*`. Do not mix in Ads API v12 tools.
3. Writes that publish, DM, delete, or hide require `confirm=true`.
4. `npm test` and `npm run typecheck` must pass.
5. Do not commit `.env` or tokens.
